<?xml version="1.0" encoding="utf-8"?>
<!--
    item_schedule_slot.xml
    Location: res/layout/item_schedule_slot.xml
    Used in: TimetableFragment → rv_schedule RecyclerView
    Shows: time label + color-accented subject card
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp">

    <!-- ── Time label ───────────────────────────────────────────── -->
    <TextView
        android:id="@+id/tv_slot_time"
        android:layout_width="40dp"
        android:layout_height="wrap_content"
        android:textSize="10sp"
        android:textColor="@color/blue_400"
        android:gravity="end"
        android:paddingTop="10dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        tools:text="8 AM" />

    <!-- ── Subject card ─────────────────────────────────────────── -->
    <androidx.cardview.widget.CardView
        android:id="@+id/card_slot"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        app:cardCornerRadius="12dp"
        app:cardElevation="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toEndOf="@id/tv_slot_time"
        app:layout_constraintEnd_toEndOf="parent"
        tools:cardBackgroundColor="@color/blue_50">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <!-- Left color accent bar — tinted per subject in adapter -->
            <View
                android:id="@+id/view_slot_accent"
                android:layout_width="4dp"
                android:layout_height="0dp"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                tools:background="@color/blue_400" />

            <!-- Subject name -->
            <TextView
                android:id="@+id/tv_slot_subject"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="10dp"
                android:layout_marginEnd="10dp"
                android:paddingTop="10dp"
                android:textSize="13sp"
                android:textStyle="bold"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toEndOf="@id/view_slot_accent"
                app:layout_constraintEnd_toEndOf="parent"
                tools:text="Physics"
                tools:textColor="@color/blue_900" />

            <!-- Room and duration -->
            <TextView
                android:id="@+id/tv_slot_detail"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="10dp"
                android:layout_marginTop="2dp"
                android:layout_marginEnd="10dp"
                android:paddingBottom="10dp"
                android:textSize="11sp"
                app:layout_constraintTop_toBottomOf="@id/tv_slot_subject"
                app:layout_constraintStart_toEndOf="@id/view_slot_accent"
                app:layout_constraintEnd_toEndOf="parent"
                tools:text="Lab B · 90 min"
                tools:textColor="@color/blue_500" />

        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.cardview.widget.CardView>

</androidx.constraintlayout.widget.ConstraintLayout>