<?xml version="1.0" encoding="utf-8"?>
<!--
    activity_dashboard.xml
    Location: res/layout/activity_dashboard.xml
    Used by: DashboardActivity.kt

    This is the HOST activity for all 5 fragments.
    It contains:
      1. NavHostFragment  — where fragments are displayed
      2. BottomNavigationView — the tab bar at the bottom

    The NavHostFragment is the container.
    Navigation Component swaps fragments inside it.
    The BottomNavigationView drives which fragment is shown.
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    android:background="@color/background_page">

    <!-- ── Fragment container ─────────────────────────────────────
         NavHostFragment hosts all 5 fragments.
         Each fragment replaces the content of this container
         when the user taps a tab or navigates.

         app:navGraph     → links to res/navigation/nav_graph.xml
         app:defaultNavHost → intercepts the system Back button
                              so pressing Back navigates within
                              the graph, not closes the activity
    ──────────────────────────────────────────────────────────── -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/nav_host_fragment"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:navGraph="@navigation/nav_graph"
        app:defaultNavHost="true"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/bottom_navigation"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- ── Bottom navigation bar ──────────────────────────────────
         app:menu  → links to res/menu/bottom_nav_menu.xml
         Each menu item id matches a Fragment id in nav_graph.xml.
         NavigationUI.setupWithNavController() in DashboardActivity
         handles the tab → fragment switching automatically.
    ──────────────────────────────────────────────────────────── -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="@color/white"
        app:menu="@menu/bottom_nav_menu"
        app:itemIconTint="@color/bottom_nav_color_selector"
        app:itemTextColor="@color/bottom_nav_color_selector"
        app:labelVisibilityMode="labeled"
        app:elevation="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>